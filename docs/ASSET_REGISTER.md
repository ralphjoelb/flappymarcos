# ASSET REGISTER — GHOST PROJECT

Inventory of every non-code asset and externally sourced component, with
provenance. Rule: anything not determinable from the repo is marked
**"Provenance unknown, human verification required"** — no sources or
licenses are invented.

## Current assets

| Asset | Location | Source/Origin | License Known? | Commercial Use? | Risk | Notes |
|---|---|---|---|---|---|---|
| Ghost protagonist sprite | code-drawn (`drawGhostSprite` in `game.js`) | Original, drawn in canvas code this session | Owned (no external asset) | Yes | NONE | Original character; hard-hat ghost silhouette is protectable as original work |
| Bebas Neue font | `index.html` via Google Fonts CDN (not bundled) | Google Fonts — Ryoichi Tsunekawa / Dharma Type | SIL OFL 1.1 (verified vs upstream + Google Fonts metadata) | Yes, incl. bundling with software if license + copyright notice accompany the font files | LOW | Record obligations in `THIRD_PARTY_LICENSES.md` |
| Space Grotesk font | `index.html via Google Fonts CDN (not bundled)` | Google Fonts — Florian Karsten (2020) | SIL OFL 1.1 (verified) | Yes, same OFL conditions | LOW | Same |
| Favicon 👻 | `index.html` data-URI SVG | Unicode emoji placeholder | n/a | n/a | NONE | Placeholder until an original app icon exists |
| All audio | synthesized at runtime (`AudioEngine`) | Original, generated by `AudioEngine` (WebAudio) | Owned | Yes | NONE | No audio files exist |
| All other art | canvas-drawn in `game.js` | Original code-drawn | Owned | Yes | NONE | Walls, peso, pylons, jeepney, skyline, ACCOUNTABILITY, etc. |
| Vercel Web Analytics | `index.html` script tag | Vercel first-party | Vendor ToS | n/a | LOW | Disclose in privacy policy |
| Upstash Redis | `api/leaderboard.js` REST calls | Vendor (env-injected credentials; no SDK; no secrets in repo) | Vendor ToS | n/a | LOW | Retention documented in privacy policy |
| Codebase | all files | Original, Claude Code-assisted | `LICENSE` (all rights reserved) | Owned | NONE | — |

## Retired assets

| Asset | Status | Reason |
|---|---|---|
| `marcos.png` (root, git-tracked) | **RETIRED 2026-09-10** — removed from repo forward; retained in git history per owner decision | AI-generated caricature of a real person (provenance: see `AI_ASSET_LOG.md`); real-person likeness incompatible with the fictional-cast rebrand |

## Housekeeping

- Untracked local dev artifacts (`test-*.png`, `v2-*.png`, `screenshot*.png`,
  `input-centered.png`, `.playwright-mcp/`, `.DS_Store`) stay out of the repo.
- `.gitignore` ignores root `*.png` but re-includes `assets/**/*.png` so future
  character art (transparent PNGs) can be committed once commissioned.
- Git history intentionally retains the retired asset and old brand (owner
  decision: keep history). The GitHub repo and Vercel domain should be
  renamed to match the new brand when convenient (owner task).